{
  "name": "hourguard",
  "private": true,
  "scripts": {
    "dev:web": "turbo run dev --filter=@hourguard/web",
    "dev:desktop": "turbo run dev --filter=@hourguard/desktop",
    "build": "turbo run build",
    "build:web": "turbo run build --filter=@hourguard/web",
    "build:desktop": "turbo run build --filter=@hourguard/desktop",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.1.0",
    "typescript": "^5.5.0"
  },
  "packageManager": "pnpm@9.9.0"
}
